#pragma once
#include <vector>

class ModSystem {
public:
	static bool are—omparable(int comparedNumber, int number, int module);
	static bool are—omparable(std::vector<int> comparedNumbers, int number, int module);
};